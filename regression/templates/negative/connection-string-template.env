# The correct pattern: the URI is configuration, the password is injected.
# The gate must not fire on this -- it is what developers are being asked to do.
DATABASE_URL=postgres://svc_app:${DB_PASSWORD}@db.internal.invalid:5432/appdb
AMQP_URL=amqps://worker:${RABBIT_PASSWORD}@broker.internal.invalid:5671/prod
